name: Base Tests > Pagefind falls back to main thread when worker unavailable
steps:
  - ref: ./background.toolproof.yml
  - step: I have a "public/ds/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>The
      beet-root</h1></body></html>
  - macro: I run Pagefind
  - step: stdout should contain "Running Pagefind"
  - step: The file "public/pagefind/pagefind.js" should not be empty
  - step: I serve the directory "public"
  - step: In my browser, I load "/"
  - step: In my browser, I evaluate {js}
    js: |-
      let pagefind = await import("/pagefind/pagefind.js");

      // Force fallback to main thread by disabling worker
      await pagefind.options({ noWorker: true });
      await pagefind.init();

      // Perform a verbose search to get execution environment info
      let search = await pagefind.search("beet-root", { verbose: true });

      toolproof.assert_eq(search.search_environment, "mainthread");
